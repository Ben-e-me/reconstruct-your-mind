// subtle, blurry, liquid rust glow — two breathing arcs forming a vertical opening.
// pure CSS (see .magic-rings in global.css).
export function MagicRings() {
  return <span className="magic-rings" aria-hidden="true" />
}
